<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>IAW_Monthly_Payment__c</fullName>
    <description>Mortgage Amount * (Mortgage Rate / 12) / (1 + (Mortgage Rate / 12) ^ -360)</description>
    <formula>IAW_Mortgage_Amount__c * 

(Mortgage_Rate__c  / 12) /
 
(1-

((1+

(Mortgage_Rate__c / 12)

) ^ -360)

)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Mortgage Amount * (Mortgage Rate / 12) / (1 + (Mortgage Rate / 12) ^ -360)</inlineHelpText>
    <label>Monthly Payment</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
